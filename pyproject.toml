[build-system]
# >=77 for PEP 639: the SPDX string form of `license` below is rejected by older
# setuptools, so the >=61 floor made the build fail on any resolver that picked one.
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "text-hygiene"
version = "0.1.0"
description = "Detect and strip invisible Unicode from text."
requires-python = ">=3.10"
dependencies = []
license = "MIT"
readme = "README.md"
keywords = ["unicode", "zero-width", "text", "linter", "bidi"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Topic :: Text Processing",
]

[project.scripts]
text-hygiene = "texthygiene.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
texthygiene = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 90

[tool.ruff.lint]
# Pinned so CI does not drift when ruff changes its defaults.
select = ["E", "F", "W", "I", "B", "UP", "RUF"]
